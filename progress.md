# 进度记录

## 2026-07-22

- 已阅读 `1.prd`、现有项目计划/发现记录及 Browser Harness 技能说明。
- 已确认小说下载功能独立放在仓库根目录，准备实现第一阶段。
- 根目录独立工具已完成首版：`library/db.py`、`library/scanner.py`、`library/app.py`、fixture 和单测。
- 离线扫描 CLI、SQLite 更新检测和单元测试通过；HTTP 验证第一次因测试 URL 未编码中文失败，准备以编码后的 query 重试。
- 编码后的 HTTP 页面/API 验证通过；Browser Harness 诊断显示当前没有 active browser connection，因此尚未触碰真实网站。
- 已补充 `README.md`、根目录 `.gitignore`，并用 `uv run python -m unittest discover -s tests -v` 验证通过（1/1）。
- 交接状态：第一阶段可离线验收；真实扫描等待用户开启已登录 Chrome 的远程调试连接。
- 已阅读 `browser-harness-troubleshooting.md`，完成当前根项目的项目范围安装：更新 `pyproject.toml`/`uv.lock`，生成 `.codex/skills/browser-harness/SKILL.md`。
- 安装验证通过：`uv run browser-harness --version` 为 `0.1.5`，模块导入正常，单元测试仍为 1/1 通过。
- 已成功只读打开 `fid=3`；页面标题为“创作者协会”，登录态正常，发现 14 个普通主题、7 个置顶主题。真实 DOM 显示回复数在 `td.num > a`、查看数在 `td.num > em`，分类标签使用 `a[href*="filter=typeid"]`。
- 用户确认页面顶部“更多”菜单提供“回复/查看”排序和时间筛选；本轮先使用网站的排序链接，不自算排名。
- 第 1 页首次真实扫描写入 14 条；发现标题选择器误取空图标链接，已修正为优先 `a.s.xst`，准备重扫确认。
- 标题修正后重扫成功；前三名为 thread 802、8997、43481，查看数排序正常。已补充多页复用同一浏览器标签和分页链接推导 `page_count`。
- 已继续完成 `fid=3` 查看数排序前 2 页扫描：34 条入库，无重复；当前数据库可直接供本地筛选页面使用。
- 用户提醒页面“更多”菜单可筛选后，已继续完成 20 页真实扫描，共 394 条；筛选页已支持动态标签多选、状态、关键词、作者和数值条件。
- 运行记录现在保存 `pages` 与 `forum_url` 到 `runs.filter_config`；测试扩展为 2/2 通过。
- 已打开 thread 802 做第二阶段只读勘察，确认付费页选择器和价格字段；尚未点击购买。
- 已新增默认 dry-run 的购买模块和 CLI；首次单测断言字符串格式不匹配，已调整断言后重跑。
- dry-run 探测确认 802 售价 1、43481 售价 1、30933 售价 2；用户补充的三个排除标签已加入购买候选过滤，dry-run“待购买”会落库为标准状态“未购买”。
- 事故记录：首次执行 `--count 2 --execute` 时 count 未传入浏览器循环，误购 6 篇（802、43481、30933、39982、29739、30703），合计 7 金币；8997/38485 已购买未重复。已停止所有购买操作，正在修复硬数量上限。
- 已修复购买数量保护：`count` 写入 Browser Harness 配置，并在成功数达到目标时立即 break；新增测试覆盖。当前不再执行购买或下载。
- 已补齐后续阶段代码：`cleaner.py`、`content.py`、`exporter.py`、`downloader.py`、`content_browser.py`、`updater.py`，并新增 `download`/`export` CLI；`download` dry-run 已验证只生成队列、不访问浏览器。
- 全部本地测试当前 5/5 通过；批量下载仍未执行。
- 用户确认 6 篇全部下载；已用明确 thread_id 选择执行正文抓取，生成 6 个按 `标题_作者名.txt` 命名的 TXT、6 个 `raw.html` 和 `metadata.json`，并更新数据库为“已下载”。
- 已生成合并预览：`runtime/six_works.txt`，总计约 68.6 万字；清洗器修正后 6 篇长度分别约 24,591 / 32,182 / 25,212 / 35,525 / 64,824 / 54,614 字。
- 用户要求无图和 `标题_作者名.txt` 命名；已完成重命名和去图，新增 QA 截图接触表 `runtime/screenshots/contact_sheet.png` 与报告 `qa_report.json`。正式流程不调用截图 QA。
- 最终回归检查通过：5/5 单元测试、`library` 编译、6 个 TXT UTF-8/命名/哈希校验均通过；首尾截图 QA 报告已生成，仅用于本次流程验收。
- 根据参考图重做本地管理页：采用浅灰背景、白色圆角卡片、扫描区/筛选区/结果表分层布局、标签胶囊、斑马纹表格和紧凑按钮；按用户要求移除右上角用户管理区。浏览器实测作者筛选从 394 条正确收敛到 10 条。
- 已从 `fid=3` 页面顶部 typeid 菜单核对出 17 个预设标签；`/api/tags` 现在返回全集，包含/排除标签均改为预设下拉选择，选中后生成可移除胶囊。浏览器实测选择“催眠NTR”后结果为 83 条。
- 根据用户补充，两个预设标签下拉框已固定在各自标签横框最右侧；管理页新增“购买设置”，支持数量、单篇最高金币、最低剩余金币、自动购买和实际购买确认。自动模式按当前排名处理，支付页预计购买后余额达到保留值即停止；未勾选实际购买时仅 dry-run。浏览器验证两个标签选择器各 18 项，单元测试 5/5 通过。
- 价格显示补充：购买探测无购买入口的帖子现在记录为“免费”；尚未探测的数据库默认 0 显示为“未探测”。重新扫描不会覆盖已探测的金币价格。已对 thread 8997 做只读 dry-run，页面确认无购买入口，管理页显示“免费”。
- 用户要求继续按 PRD 完成。已恢复文件化计划并盘点剩余缺口：底层下载/清洗/导出已有实现，主要工作是接通前端/API、补齐余额不足和错误状态、记录运行结果，并保持真实下载默认关闭。
- 已接通 `/api/download` 与前端“下载勾选/下载当前筛选”入口；默认 dry-run。对 thread 8997 的 HTTP 预览返回 1 篇“待下载”，确认未调用浏览器正文抓取。
- 已接通 `/api/export` 与 ZIP/TXT 格式选择；用已下载的 thread 802、43481 生成 ZIP，压缩包包含两篇独立的 `标题_作者名.txt`。
- 新增 `/api/runs`，下载预览已写入 runs：`sort_type=download`、`stop_reason=预览完成`、`skipped_count=1`。
- 浏览器最终验收时 `uv run browser-harness --doctor` 显示 Chrome 正常但 active connections 为 0；遵循项目技能约束未启动替代浏览器，转为继续 HTTP 与静态页面验证。
- 已完善运行与错误状态：`content_browser.py` 增加免费/已购买回写、金币超限、余额不足、购买失败、空页面/空正文检测；抓取结果统一写回 `works`，更新帖会覆盖本地快照并清除 `has_update`。
- 页面新增标题关键词、下载设置、四个下载/导出按钮和最近运行卡片。Node 静态编译 5 个页面脚本全部通过，必需控件与三个 API 路径均存在。
- HTTP 验证：未勾选下载返回 400 安全提示；状态=已下载的当前筛选结果成功合并导出 6 篇 TXT（约 685 KB）；标题关键词“觉醒芯片”准确返回 thread 8997。
- Browser Harness 连接恢复后完成本地页面交互：页面加载 394 条、5 条最近运行记录，四个下载/导出按钮及下载设置均存在。标题关键词筛选得到 1 条，勾选后 dry-run 显示“队列 1 篇，未访问帖子”；状态=已下载筛选得到 6 条，并从前端成功导出 6 篇 ZIP。
- 最终视觉 QA 截图保存为 `runtime/prd_complete_ui.png`；页面卡片顺序为扫描设置、购买设置、下载设置、结果筛选、最近运行和结果表，预设标签仍固定在横框最右侧。
- 最终回归：`library` 编译通过，单元测试 5/5，通过 Node 编译全部 5 个页面脚本，`/api/runs` 健康检查返回 200。未执行真实批量下载或新购买。
- 用户进一步明确实际使用方式：按查看/回复排序后分批下载，下载时自动购买；可见限制只保留本批下载篇数，金币不足自动停止。独立购买数量、最低余额、确认购买和开始购买面板需要隐藏，但底层购买功能保留。
- 已按简化流程调整：独立购买面板通过 `hidden` 隐藏，源码注释明确记录用户要求且 `/api/purchase` 与原面板 DOM/JS 未删除；下载设置只显示“本次下载篇数”。
- 前端下载按钮现在直接发送 `execute=true`，购买价格上限在此流程设为 1000000（等同取消单篇上限），点击时仅保留一次确认提示；处理达到批次篇数或检测到余额不足即停止。
- Browser Harness 可视化确认：可见卡片不再包含购买设置，排序选项为“查看数降序/回复数降序”，下载区只显示本次下载篇数；截图为 `runtime/simple_download_ui.png`。未点击下载按钮，未触发真实任务。
- 用户修正自动下载规则：恢复单篇最高金币和最低保留余额；新增忽略筛选、固定按最新查看数排序的自动下载入口；已完整下载作品永久跳过，未完成作品允许续传；作者改为数据库预设下拉选择。开始实施，验收阶段禁止点击真实下载按钮。
- 已完成主流程实现并通过 5/5 单测、Python 编译和 6 段页面脚本 Node 语法检查。HTTP dry-run 自动预览在故意传入不可能筛选条件时仍返回查看数最高的 5 个未下载作品，证明自动模式忽略筛选；数据库前 6 名已下载作品均被跳过。Browser Harness 仅查看页面和截图，未点击任何下载按钮。
- 最终服务已重启到最新代码。HTTP dry-run 再次核对自动队列前三篇为 8997、38485、26804，与数据库 `views DESC` 的未下载队列完全一致；请求中的“已下载/不存在作者/超高查看数”筛选被正确忽略。Browser Harness 最终确认自动下载按钮、三项限制、159 个作者选项（含全部作者）可见，购买面板隐藏；视口截图更新为 `runtime/auto_download_ui_viewport.png`。全程未触发真实购买或下载。
- 用户新增全局限制：同一帖子最多访问 6 个分页，并可在下载设置中调整。用户随后明确不是待续传；最终按“截断完成”处理，达到上限即保存现有内容并标记已下载，后续页不再访问。
- 已完成最终修正：下载设置新增“单帖最多页数”，默认 6，允许 1–100 并通过浏览器 localStorage 保留选择；所有下载模式共用。抓取结果记录实际保存页数、源帖子页数及是否触发上限，达到上限仍返回“已抓取”并由数据库标记“已下载”。5/5 单测、Python 编译和 7 段页面脚本检查通过；Browser Harness 只读确认界面无“待续传”选项，截图为 `runtime/max_pages_ui.png`，未启动真实下载。
- 用户要求调查自动下载“成功 5 / 失败 4”。已只读核对 run 13 与 4 个失败作品：3 个当前已无购买链接，疑似实际购买成功但被 1.5 秒后的旧页面校验误判；26366 仍有购买链接。未重试、未修改购买状态、未新增购买。
- 用户要求解决上述购买误判与页面不稳定情况。开始实现：等待付款表单/提交按钮、页面加载重试、点击后多轮只读验证（不重复提交）、无购买链接时结合已知价格判断“已购买”而非误判免费，并把逐项 reason/attempts 写入运行记录。
- 已完成购买重试修复并通过 5/5 单测、Python 编译、Browser Harness 程序编译和 7 段页面脚本检查。只读筛选得到 34186（3 金币）与 38686（1 金币），用户授权后仅对这两篇执行真实测试；run 17 成功 2 / 失败 0，生成完整本地快照。管理页最近运行正确显示该记录，隐藏购买面板仍不可见，截图为 `runtime/purchase_retry_ui.png`。
