# 项目协作与 Git 约定

- 本项目不需要联合开发或多代理协作；默认由当前代理直接完成任务。
- 用户明确要求上传代码时，默认提交到 `main` 分支并推送到已配置的 `origin` 远程仓库。
- 除非用户另有说明，不创建工作分支、不发起 PR，也不把修改停留在未提交状态。
- 提交前先运行项目测试和编译检查，并确认没有误改数据库或浏览器 profile。
- 不要提交运行时数据库、下载内容、导出文件或浏览器缓存；遵守 `.gitignore`。
